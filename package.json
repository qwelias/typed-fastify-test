{
    "name": "typed-fastify-test",
    "version": "0.0.0",
    "scripts": {
        "build": "tfs gen src/schema.ts && tsc --build .",
        "dev": "npm run build && node --inspect-brk dist | pino-pretty",
        "start": "node dist"
    },
    "dependencies": {
        "@coobaha/typed-fastify": "^1.0.1",
        "@fastify/swagger": "^7.4.1",
        "@types/node": "^18.6.4",
        "fastify": "^4.3.0",
        "mongodb": "^4.8.1",
        "typescript": "^4.7.4"
    },
    "devDependencies": {
        "pino-pretty": "^7.5.3",
        "ts-json-schema-generator": "^1.0.0",
        "typescript-json-schema": "^0.54.0"
    }
}
